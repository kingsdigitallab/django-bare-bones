define([
    'module',
    'jquery',
    'foundation'
], function(module, $, fn) {
    'use strict';

    $(document).ready(function() {
        // loads foundation
        $(document).foundation();
    });

    return module;
});
